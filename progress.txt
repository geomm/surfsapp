## Codebase Patterns
- UI typecheck: run `yarn typecheck` from ui/ (no root package.json). It runs `vue-tsc --noEmit`.
- Lit components registered via `./components/lit/index` import in ui/src/main.ts.
- Pinia uses Options API `defineStore` style for this project.
- IndexedDB via Dexie: `ui/src/db.ts` exports `db` (SurfsAppDB) with `favourites` (&beachId) and `settings` (&key) tables. Import as `import { db } from '../db'` from stores/components.
- Vue parent `onMounted` fires AFTER child `onMounted`, so any startup work that must complete BEFORE child components mount (e.g. hydrating store from IndexedDB before HomeView's fetchBeaches) must run in main.ts before `app.mount()`, not in App.vue's onMounted.
- To use a Pinia store inside main.ts (before mount), pass the pinia instance explicitly: `useBeachStore(pinia)`.
- PWA assets generated via `yarn generate-pwa-assets` (runs `pwa-assets-generator`) in ui/; config in ui/pwa-assets.config.ts. Source SVG is ui/public/logo.svg.
- Default maskable asset name from @vite-pwa/assets-generator is `maskable-icon-...png`; override via preset.assetName to emit `pwa-maskable-...png` if the manifest expects that convention.

## 2026-04-08 - US-001
- Installed pinia in ui/ and registered createPinia() in ui/src/main.ts.
- Created ui/src/types/beach.ts (Beach interface).
- Created ui/src/stores/beachStore.ts with state (beaches, loading, error, favourites), actions (fetchBeaches, toggleFavourite), getters (sortedBeaches, favouriteBeaches). API_BASE constant at top.
- Files changed: ui/package.json, ui/yarn.lock, ui/src/main.ts, ui/src/types/beach.ts, ui/src/stores/beachStore.ts
- **Learnings for future iterations:**
  - Branch ralph/phase-6-beach-listing-view created from main.
  - Run `yarn typecheck` from repo root, not from ui/.
  - sortedBeaches getter must place null scores last (not sort them as 0).
---

## 2026-04-08 - US-003
- Extended Beach interface with optional forecast fields.
- beachStore.fetchBeaches now parallel-fetches /beaches/:id/forecast per beach and extracts first hourly rawData + today's dailySummary; failures/missing fields handled gracefully.
- Added ui/src/utils/compass.ts (degreesToCompass 8-point).
- HomeView renders conditions row (<surf-icon waves> swell Xm · Xs · DIR, <surf-icon wind> X km/h DIR), best window 'Best: HH:MM–HH:MM', or 'No forecast data' fallback.
- Files changed: ui/src/types/beach.ts, ui/src/stores/beachStore.ts, ui/src/utils/compass.ts, ui/src/views/HomeView.vue
- **Learnings for future iterations:**
  - Backend forecast response shape assumed: { hourlyForecasts: [{ rawData: {...} }], dailySummaries: [{ date, bestWindowStart, bestWindowEnd }] }. Verify when backend is running.
  - Manual browser verification still needed — backend not running during typecheck-only pass.
---

## 2026-04-08 - US-004
- Added ui/src/utils/time.ts with formatRelativeTime and isStale helpers.
- HomeView shows staleness line per card; warn color (var(--color-surf-maybe)) when >6h old; 'No data yet' when lastUpdated is null.
- Files changed: ui/src/utils/time.ts, ui/src/views/HomeView.vue
- **Learnings for future iterations:**
  - Typecheck passed. Manual browser verification still needed.
---

## 2026-04-08 - US-002
- Replaced ui/src/views/HomeView.vue with beach listing: header (title + refresh icon), loading/error/retry states, sortedBeaches rendered as <surf-card clickable> list with name, region, <surf-badge> (variant=currentLabel or 'neutral' when null, score or 'No data' text), skillLevel tag.
- Uses beachStore.fetchBeaches() onMounted; retry button re-fetches.
- Mobile-first layout, max-width 600px centered, var(--space-4) gap between cards.
- Dev routes /design and /components still in router.ts but no longer linked.
- Files changed: ui/src/views/HomeView.vue
- **Learnings for future iterations:**
  - Lit custom elements work in Vue templates without additional config; vue-tsc accepts them.
  - Backend wasn't running locally during implementation; typecheck-only verification. Manual browser verification still needed.
  - surf-card dispatches 'card-click' custom event (composed, bubbles) when clickable is set.
  - surf-badge variant union includes 'neutral' — use it for null currentLabel.
---

## 2026-04-08 - US-006
- Added ui/src/composables/usePullToRefresh.ts — touch-based pull-to-refresh with dampened pull distance, 60px threshold, and a trigger() function reused by the header refresh button.
- HomeView: wired scrollRoot ref as the touch target, shows a <surf-icon refresh-cw> indicator that expands with pullDistance while pulling and spins while refreshing; header refresh button also spins and is disabled during refresh.
- Desktop (no touch) only uses the header button — touch listeners guarded with 'ontouchstart' in window check.
- Files changed: ui/src/composables/usePullToRefresh.ts, ui/src/views/HomeView.vue
- **Learnings for future iterations:**
  - Typecheck passes. Manual browser verification still needed (touch gesture best tested on device or Chrome devtools touch emulation).
---

## 2026-04-09 - US-002 (Phase 7)
- Added lazy /beaches/:id route (name: beach-detail) in ui/src/router.ts.
- Created ui/src/views/BeachDetailView.vue scaffold: header with back button (router.back() or /), beach name title, onMounted fetchBeachDetail, onBeforeUnmount clearSelectedBeach, loading + error/retry states, mobile-first max-width 600px layout.
- HomeView surf-card now @click="openBeach(beach)" pushing to beach-detail route.
- Files changed: ui/src/router.ts, ui/src/views/BeachDetailView.vue, ui/src/views/HomeView.vue
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed.
  - Router file lives at ui/src/router.ts so detail view import path is './views/BeachDetailView.vue' (PRD snippet used '../views/...' which would be wrong).
  - Vue @click on a Lit <surf-card clickable> works — the card bubbles a native click in addition to the card-click custom event.
---

## 2026-04-09 - US-001 (Phase 7)
- Extended ui/src/types/beach.ts with HourlyForecast, DailySummary, ForecastSnapshot interfaces.
- Extended beachStore state with selectedBeach, selectedForecast, detailLoading, detailError.
- Added fetchBeachDetail(id) action: seeds selectedBeach from cached list, then Promise.all parallel fetches /beaches/:id and /beaches/:id/forecast.
- Added clearSelectedBeach() action.
- Branch ralph/phase-7-beach-detail-view created from main.
- Files changed: ui/src/types/beach.ts, ui/src/stores/beachStore.ts
- **Learnings for future iterations:**
  - Typecheck: `cd ui && yarn typecheck` passes.
  - Backend GET /beaches/:id assumed to return a Beach object directly (matches phase-6 list shape).
---

## 2026-04-08 - US-005
- Added heart favourite button to each beach card in HomeView; uses @click.stop to avoid card-click. Styled via .fav-btn (44x44 touch target) and .fav-btn-on (red + bold).
- Reactivity works through pinia store's `favourites` Set via isFavourite() helper calling beachStore.favourites.has.
- Files changed: ui/src/views/HomeView.vue
- **Learnings for future iterations:**
  - Pinia Set state is reactive in templates when read inside a function (re-evaluated on render).
  - Typecheck passes. Manual browser verification still needed.
---

## 2026-04-09 - US-004 (Phase 7)
- Created ui/src/components/lit/surf-disclosure.ts: reactive `summary` + `open` (reflected) props, header with default/named 'summary' slot, chevron-right icon rotating 90° when open, body max-height+opacity 200ms transition, dispatches composed/bubbling 'toggle' event with {open}.
- Registered in ui/src/components/lit/index.ts (imported once by main.ts via the barrel).
- Added closed + open examples to ComponentsPreview (/components gallery).
- Files changed: ui/src/components/lit/surf-disclosure.ts, ui/src/components/lit/index.ts, ui/src/views/ComponentsPreview.vue, prd.json
- **Learnings for future iterations:**
  - Typecheck passes. Manual browser verification still needed.
  - Lit components are registered via the ui/src/components/lit/index.ts barrel (imported once in main.ts) — add new components there, not directly in main.ts.
  - `reflect: true` on boolean props lets CSS use `:host([open])` selectors for state-driven styling.
---

## 2026-04-09 - US-005 (Phase 7)
- Added ui/src/utils/reasons.ts with classifyReason(reason, score).
- BeachDetailView renders <surf-disclosure summary="Why this score"> beneath the hero; pros/cons grouped from selectedForecast.hourlyForecasts[0].reasons; ✓ / ✕ unicode icons colored via --color-surf-very-good / --color-surf-poor; empty groups hidden; 'No detailed reasons available' fallback.
- Files changed: ui/src/utils/reasons.ts, ui/src/views/BeachDetailView.vue, prd.json
- **Learnings for future iterations:**
  - Typecheck passes. Manual browser verification still needed.
---

## 2026-04-09 - US-006 (Phase 7)
- Installed maplibre-gl in ui/, imported maplibre-gl.css in ui/src/main.ts.
- Created ui/src/components/BeachMap.vue with props coords + name, demo style, marker + popup, 240px container, destroy on unmount, 'Map unavailable' fallback on init error.
- BeachDetailView renders <BeachMap> between the reasons disclosure and the (future) forecast strip.
- Files changed: ui/package.json, ui/yarn.lock, ui/src/main.ts, ui/src/components/BeachMap.vue, ui/src/views/BeachDetailView.vue, prd.json
- **Learnings for future iterations:**
  - Typecheck passes. Manual browser verification still needed.
  - BeachDetailView is already lazy-loaded via dynamic import in router.ts, so MapLibre's bundle only hits the detail route.
---

## 2026-04-09 - US-003 (Phase 7)
- BeachDetailView hero block: name, region, large score number next to <surf-badge>, favourite heart (44x44, red when active) absolute top-right, staleness line with 6h warn color, 'No data'/'No data yet' fallback.
- Reused formatRelativeTime/isStale and favourites pattern from HomeView.
- Files changed: ui/src/views/BeachDetailView.vue, prd.json
- **Learnings for future iterations:**
  - Typecheck passes. Manual browser verification still needed.
  - --font-size-2xl not confirmed to exist; used fallback var(--font-size-2xl, var(--font-size-xl)).
---

## 2026-04-11 - US-002 (Phase 8)
- beachStore.toggleFavourite now persists to IndexedDB: db.favourites.put({ beachId }) on add, db.favourites.delete(beachId) on remove.
- Fire-and-forget writes (no await); errors caught via .catch(console.error) so unhandled promise rejections don't bubble.
- Pinia Set remains the immediate source of truth — UI reactivity unchanged.
- Files changed: ui/src/stores/beachStore.ts
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes.
  - Dexie's put/delete return Promises; keep them unawaited but always attach .catch to avoid unhandled rejections.
---

## 2026-04-11 - US-001 (Phase 8)
- Branch ralph/phase-8-favourites created from main.
- Installed dexie@4 (yarn add dexie) in ui/.
- Created ui/src/db.ts exporting a SurfsAppDB Dexie subclass (database name: 'surfsapp') with version 1 schema: favourites (&beachId) and settings (&key). Tables typed via Table<FavouriteRecord, string> / Table<SettingRecord, string> class fields so db.favourites and db.settings are fully typed.
- Exported FavouriteRecord and SettingRecord interfaces from the same module.
- Files changed: ui/package.json, ui/yarn.lock, ui/src/db.ts
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes.
  - Dexie Table generic takes (RecordType, PrimaryKey) — use `&beachId` / `&key` for unique string primary keys.
  - The settings table is created in v1 even though it isn't used until Phase 11 to avoid a future Dexie version bump.
---

## 2026-04-09 - US-007 (Phase 7)
- Extended ui/src/utils/time.ts with formatWeekday + formatDayOfMonth helpers.
- Created ui/src/components/ForecastStrip.vue: horizontally scrollable scroll-snap strip of <surf-card> days with weekday, day-of-month, <surf-badge> peakScore/overallLabel, best window 'HH:MM–HH:MM' or '—'. Empty/null state → 'No forecast available'.
- BeachDetailView renders <ForecastStrip> beneath <BeachMap> when selectedForecast has dailySummaries.
- Files changed: ui/src/utils/time.ts, ui/src/components/ForecastStrip.vue, ui/src/views/BeachDetailView.vue, prd.json
- **Learnings for future iterations:**
  - Typecheck passes. Manual browser verification still needed.
  - Phase 7 Beach Detail View is now complete — all 7 stories pass.
---

## 2026-04-11 - US-004 (Phase 8)
- beachStore: added showFavouritesOnly state (default false), displayedBeaches getter (filters sortedBeaches to favourites when showFavouritesOnly), and toggleFavouritesFilter() action.
- HomeView header: added heart <surf-icon> toggle button between the title and the refresh button with aria-pressed + aria-label; active state uses --color-surf-poor, inactive uses --color-text-secondary. 44x44 touch target. Filter button uses margin-left: auto so it sits flush with the refresh button.
- HomeView v-for now iterates over beachStore.displayedBeaches instead of sortedBeaches.
- Files changed: ui/src/stores/beachStore.ts, ui/src/views/HomeView.vue, prd.json
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed.
  - Pinia options-API getters can reference other getters via `this.sortedBeaches` — typing works as long as the referenced getter has an explicit return type annotation.
  - Header flex layout already used `justify-content: space-between`; adding `margin-left: auto` to a middle item keeps it grouped with the rightmost item without disturbing the title.
---

## 2026-04-11 - US-005 (Phase 8)
- HomeView: added empty state block shown when showFavouritesOnly is true AND displayedBeaches is empty. Contains surf-icon heart (48px), h2 'No favourites yet', subtext 'Tap the heart on any beach to save it here', and a secondary surf-button 'Show all beaches' wired to beachStore.toggleFavouritesFilter().
- Empty state rendered via v-else-if between the error branch and the beach list v-else; centered with flex column, --space-6 padding, min-height 60vh so it feels visually anchored in the content area.
- Files changed: ui/src/views/HomeView.vue, prd.json
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed (toggle filter with no favourites saved, confirm empty state appears; click 'Show all beaches' to confirm it clears the filter).
  - Phase 8 Favourites is now complete — all 5 stories pass.
---

## 2026-04-11 - US-003 (Phase 8)
- Added beachStore.hydrateFavourites() action: reads all records from db.favourites, populates the in-memory favourites Set; on error, logs and continues with an empty Set.
- Wired hydration in ui/src/main.ts BEFORE app.mount(): instantiates the store with the pinia instance, calls hydrateFavourites(), then mounts via .finally() so a hydration failure still mounts the app.
- Approach chosen because Vue parent onMounted runs AFTER children — calling it from App.vue would race HomeView's fetchBeaches and cause a flash of unfavourited hearts.
- Files changed: ui/src/stores/beachStore.ts, ui/src/main.ts, prd.json
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed (favourite a beach, refresh, confirm heart is still filled).
  - To use a Pinia store before app.mount(), pass the pinia instance: `useBeachStore(pinia)`.
  - Use `.finally()` (not `.then()`) on hydration so transient IndexedDB errors don't prevent the app from mounting.
---

## 2026-04-11 - US-002 (Phase 9)
- Extended ui/src/db.ts with Dexie v2 schema: added beachesCache (&id) and forecastsCache (&beachId) tables. v1 stores (favourites, settings) repeated in the v2 version() call per Dexie requirements.
- Added BeachCacheRecord and ForecastCacheRecord type aliases that reuse Beach and ForecastSnapshot from types/beach.ts (the cache records are shape-identical to the API types).
- Typed db.beachesCache and db.forecastsCache as Table<..., string> class fields.
- Files changed: ui/src/db.ts, prd.json
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes.
  - Dexie v2 upgrade added without an upgrade() callback — new stores don't need data migration.
  - Type aliases (not re-declarations) for cache records keep the shapes in sync with the API types automatically.
---

## 2026-04-11 - US-003 (Phase 9)
- beachStore.fetchBeaches: refactored the per-beach map callback to return { beach, forecast } tuples instead of just an enriched Beach — the raw ForecastSnapshot is captured before swell/wind extraction so it can be cached. After enrichment, db.beachesCache.bulkPut(enriched) writes all beaches; for each result with a non-null forecast, db.forecastsCache.put(forecast) writes the snapshot. All writes are fire-and-forget with .catch(console.error).
- beachStore.fetchBeachDetail: added fire-and-forget db.beachesCache.put(beach) and db.forecastsCache.put(forecast) after the successful API responses.
- Forecast responses now typed as ForecastSnapshot (was untyped); the beachId field is populated by the backend so cached records satisfy the &beachId primary key in db.forecastsCache.
- Files changed: ui/src/stores/beachStore.ts, prd.json
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed.
  - Dexie bulkPut/put return Promises — keep them unawaited so cache writes never block UI rendering, but always attach .catch to avoid unhandled rejections (same pattern as the Phase 8 favourites writes).
  - When an enrichment step transforms the API response and discards part of it, return a tuple { transformed, raw } from the map callback so callers can still access the raw payload (here: for IndexedDB caching).
  - The `first` rawData object needs an explicit `Record<string, number>` type annotation because `?? {}` widens the union and otherwise loses the index signature.
---

## 2026-04-11 - US-004 (Phase 9)
- Extracted enrichBeachWithForecast(beach, forecast) helper at module scope in beachStore.ts so enrichment logic is identical for network and cached data paths.
- beachStore.fetchBeaches: on network error, reads db.beachesCache.toArray(); for each cached beach, reads matching forecast from db.forecastsCache.get(beach.id) and enriches via the shared helper. If cache is empty, sets this.error to 'No internet connection and no cached data available'. On successful cache hydration, clears this.error.
- beachStore.fetchBeachDetail: on network error, Promise.all reads db.beachesCache.get(id) + db.forecastsCache.get(id). If no cached beach, sets detailError to 'No internet connection and no cached data for this beach'. Otherwise sets selectedBeach + selectedForecast from cache and clears detailError.
- Files changed: ui/src/stores/beachStore.ts, prd.json
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed (toggle devtools offline mode, refresh, confirm cached data renders with no error banner).
  - The outer try/catch's `err` variable is accessible from within a nested try/catch inside the catch block — useful as a final fallback when the cache read itself also fails.
  - enrichBeachWithForecast is a pure module-scope helper (not a store method) because it doesn't need `this` and keeping it outside the Pinia options object avoids `this` typing headaches in Options-API stores.
---

## 2026-04-11 - US-001 (Phase 9)
- Branch ralph/phase-9-offline-first created from main.
- Installed vite-plugin-pwa@1.2.0 as a dev dependency in ui/.
- Updated ui/vite.config.ts: imported VitePWA and added to plugins array with registerType 'prompt', workbox globPatterns ['**/*.{js,css,html,ico,png,svg,woff2}'], and a minimal manifest (name/short_name 'surfsapp', theme_color '#1a73e8', background_color '#ffffff', display 'standalone'). No icons included (Phase 10).
- Created ui/src/vite-env.d.ts with /// <reference types="vite/client" /> and /// <reference types="vite-plugin-pwa/vue" /> so future virtual:pwa-register/vue imports typecheck.
- Files changed: ui/package.json, ui/yarn.lock, ui/vite.config.ts, ui/src/vite-env.d.ts, prd.json
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes.
  - The ui/ project previously had no vite-env.d.ts — created one that includes both vite/client and vite-plugin-pwa/vue references.
  - registerType 'prompt' is required for the US-006 update-banner UX (the new SW waits for user action before activating).
  - Service worker is only registered in production builds (yarn build && yarn preview) — dev mode will not register it, so US-005/US-006 browser verification must run against a production build.
---

## 2026-04-11 - US-006 (Phase 9)
- Created ui/src/composables/useServiceWorker.ts: wraps useRegisterSW from virtual:pwa-register/vue, exposes needRefresh ref + updateServiceWorker function, logs onRegisterError.
- App.vue: imported useServiceWorker and added a second fixed-top banner (.update-banner) rendered via <Transition name="banner"> that reuses the existing slide-down/up CSS. Blue background (var(--color-primary, #1a73e8)), white 'New version available' text and a <surf-button variant="secondary" size="sm"> Refresh button that calls updateServiceWorker(true). Offline takes priority: showUpdateBanner is gated by `needRefresh && isOnline`, so when offline the update banner is hidden even if a new SW is waiting.
- The app-shell--banner padding-top is now driven by a shared showAnyBanner computed so either banner pushes the page content down by 40px.
- The update banner's surf-button overrides --color-primary to #ffffff so the secondary variant's text/border render white on the blue banner background.
- Files changed: ui/src/composables/useServiceWorker.ts, ui/src/App.vue, prd.json
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification requires a production build: `cd ui && yarn build && yarn preview`, then make a code change, rebuild, and refresh twice to see the update banner. In dev mode needRefresh is always false.
  - The vite-plugin-pwa/vue virtual module import only typechecks because ui/src/vite-env.d.ts has `/// <reference types="vite-plugin-pwa/vue" />` (added in US-001).
  - surf-button's secondary variant uses --color-primary for both its text and border; override the CSS custom property at the use site (`--color-primary: #ffffff`) to restyle it on coloured backgrounds rather than forking the component.
  - Phase 9 Offline First is now complete — all 6 stories pass.
---

## 2026-04-11 - US-005 (Phase 9)
- Created ui/src/composables/useOnlineStatus.ts: exports isOnline reactive ref seeded from navigator.onLine, subscribes to window online/offline events, cleans up listeners via onScopeDispose.
- Replaced ui/src/App.vue template-only shell with a <script setup> that uses useOnlineStatus and renders a fixed-top offline banner inside a <Transition name="banner">. Banner is amber (--color-surf-maybe / #f59e0b), white bold 40px tall, z-index 1000, text 'You're offline — showing cached data'. Slide-down/up transform+opacity transition at 200ms. App shell div below gets padding-top: 40px when offline (transition on padding-top).
- Files changed: ui/src/composables/useOnlineStatus.ts, ui/src/App.vue, prd.json
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed (DevTools offline toggle — banner should slide down and content should push down, not be overlapped).
  - onScopeDispose from 'vue' works inside composables without an explicit component context; it ties cleanup to the current effect scope (in this case the setup function of App.vue).
  - App.vue was previously template-only with just <RouterView />; this is the first time it holds app-chrome. Future banners (US-006 update banner) should render in this same location and share the 40px fixed-top banner pattern.
---

## 2026-04-18 - US-002 (Phase 10)
- Expanded ui/vite.config.ts VitePWA manifest from minimal (name/short_name/theme/bg/display) to the full Phase 10 set: name 'surfsapp — Greek Surf Conditions', description, orientation 'portrait', scope '/', start_url '/', categories ['sports','weather','travel'], lang 'en', and a 4-entry icons array (pwa-192/512 'any' + pwa-maskable-192/512 'maskable').
- Verified by running `yarn build` — dist/manifest.webmanifest contains all expected fields and resolves the four PNGs generated in US-001.
- Files changed: ui/vite.config.ts, prd.json
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes; `yarn build` emits dist/manifest.webmanifest cleanly.
  - VitePWA's `manifest` option accepts the standard W3C manifest fields directly — no plugin-specific transformations needed.
  - The icons array `src` paths are relative to the public directory root, so `'pwa-192x192.png'` (no leading slash) maps to `/pwa-192x192.png` at runtime.
  - Phase 9's minimal manifest had no `description`, `orientation`, `scope`, `start_url`, `categories`, `lang`, or `icons` — Phase 10 adds them. Manual browser verification still needed (open DevTools → Application → Manifest in a `yarn preview` session and confirm zero warnings).
---

## 2026-04-19 - US-001 (Phase 11)
- Branch ralph/phase-11-filters created from main.
- Extended beachStore state with selectedRegions: Set<string> and selectedDifficulties: Set<string>.
- displayedBeaches getter rewritten as a single filter chain: region AND difficulty AND favourites; empty Sets mean no filter.
- Added getters: activeFilterCount, availableRegions, availableDifficulties.
- Added actions: toggleRegion, toggleDifficulty, clearFilters (clears regions, difficulties, AND showFavouritesOnly), hydrateSettings (reads the 3 keys from db.settings with defensive fallbacks).
- Extended toggleFavouritesFilter to persist to db.settings under key 'showFavouritesOnly'.
- Files changed: ui/src/stores/beachStore.ts, prd.json.
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes.
  - SettingRecord stores `value: unknown` — hydrateSettings must type-narrow (Array.isArray + every typeof 'string' for arrays; typeof 'boolean' for the favourites flag) before populating state.
  - All db.settings.put() writes are fire-and-forget with .catch(console.error), matching the Phase 8 favourites persistence pattern.
  - Pinia options-API Set mutations (.add/.delete) trigger reactivity without needing to reassign the Set (matches existing `favourites` toggle pattern).
---

## 2026-04-18 - US-001 (Phase 10)
- Branch ralph/phase-10-pwa-installation created from main.
- Installed @vite-pwa/assets-generator@1.0.2 as dev dependency in ui/.
- Created ui/public/logo.svg: 512x512 viewBox, rounded-square background #1a73e8, two stacked white wave silhouettes (the back wave at 0.65 opacity for depth). No gradients, no text, geometric Bézier curves only. 15 lines.
- Created ui/pwa-assets.config.ts using minimal2023Preset spread + override maskable sizes to [192, 512] (the preset only ships 512) and override assetName to emit pwa-maskable-* instead of the default maskable-icon-* prefix. Source image public/logo.svg, headLinkOptions preset '2023'.
- Added "generate-pwa-assets": "pwa-assets-generator" script to ui/package.json.
- Running yarn generate-pwa-assets produced ui/public/: pwa-64x64.png (bonus from preset), pwa-192x192.png, pwa-512x512.png, pwa-maskable-192x192.png, pwa-maskable-512x512.png, favicon.ico, apple-touch-icon-180x180.png.
- Files changed: ui/package.json, ui/yarn.lock, ui/public/logo.svg, ui/public/*.png, ui/public/favicon.ico, ui/pwa-assets.config.ts, prd.json.
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes.
  - @vite-pwa/assets-generator v1 does NOT export createAppIconsOnlyPreset; use minimal2023Preset and customise. The preset exports live in @vite-pwa/assets-generator/config (alongside defineConfig), NOT the root package.
  - minimal2023Preset produces pwa-64x64 in addition to 192/512 (the preset's transparent.sizes = [64, 192, 512]). Keep or filter depending on manifest expectations.
  - sharp-ico bundles its own libvips which clashes at runtime with the top-level @img/sharp-libvips (prints an objc class duplicate warning). Cosmetic — generator still runs cleanly.
  - The generator also prints ready-to-paste <link> tags for the head and a manifest icons JSON snippet — capture these for US-002 / US-003.
---

## 2026-04-19 - US-002 (Phase 11)
- ui/src/main.ts: replaced the single `hydrateFavourites().finally(mount)` chain with `Promise.allSettled([hydrateFavourites(), hydrateSettings()]).finally(mount)` so filter state is hydrated alongside favourites before the first render.
- allSettled chosen (over Promise.all) because either hydration is already try/catch'd internally; allSettled just ensures one rejecting won't prevent the other from running — `.finally(mount)` guarantees the app mounts either way, matching the US-003 (Phase 8) pattern.
- Files changed: ui/src/main.ts, prd.json.
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed (toggle favourites-only, reload, confirm filter stays applied; after US-003 repeat with region/difficulty filters).
  - main.ts is the canonical hydration site for pre-mount store warmup — future persisted-state features should add another hydrate call into the same Promise.allSettled array rather than re-introducing an App.vue onMounted hook (Vue parent onMounted runs AFTER children, so it would race HomeView's fetchBeaches).
---

## 2026-04-19 - US-004 (Phase 11)
- ui/src/components/lit/surf-icon.ts: registered the 'sliders-horizontal' icon (imported SlidersHorizontal from lucide).
- HomeView.vue: imported FilterSheet and added local `filterSheetOpen = ref(false)`. Added a new <button class="filter-btn"> between the favourites toggle and the install button: surf-icon name="sliders-horizontal", aria-label="Open filters", 44x44 touch target, transparent background; clicking sets filterSheetOpen.value = true. A small <span class="filter-badge"> dot appears top-right of the icon when beachStore.activeFilterCount > 0 (var(--color-surf-very-good), absolutely positioned). Rendered <FilterSheet :open="filterSheetOpen" @close="filterSheetOpen = false" /> at the end of the template.
- Files changed: ui/src/components/lit/surf-icon.ts, ui/src/views/HomeView.vue, prd.json.
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed (open sheet, pick a region, confirm dot badge appears).
  - lucide exports SlidersHorizontal (PascalCase); the surf-icon registry key uses kebab-case ('sliders-horizontal').
  - The filter button must be placed BETWEEN .fav-filter-btn and .install-btn — .fav-filter-btn already has `margin-left: auto` so the new button naturally groups with the rightmost cluster without further layout tweaks.
  - The badge dot is a span with `position: absolute` inside a `position: relative` button; an empty span styled as a circle is enough — no count text per the PRD.
---

## 2026-04-19 - US-005 (Phase 11)
- HomeView.vue: tightened the existing favourites-only empty state condition to also require `selectedRegions.size === 0 && selectedDifficulties.size === 0` so it only fires when favourites-only is the SOLE active filter — prevents regression where the heart-empty state would mask a region/difficulty-driven empty list.
- Added a new v-else-if 'No beaches match your filters' empty state directly after it: <surf-icon name="sliders-horizontal" size="48">, h2, p, and a secondary <surf-button> wired to beachStore.clearFilters(). Reuses the existing .empty-state class so visual styling matches the favourites empty state exactly.
- Condition for the new block: beaches.length > 0 AND displayedBeaches.length === 0 AND activeFilterCount > 0 AND (selectedRegions.size > 0 OR selectedDifficulties.size > 0). The last clause guarantees the new state ONLY fires when a region/difficulty is contributing — the favourites-alone case is still handled by the prior block.
- Phase 11 Filters is now complete — all 5 stories pass.
- Files changed: ui/src/views/HomeView.vue, prd.json.
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed (pick a region+difficulty combination that no beach satisfies, confirm new state appears; click 'Clear filters', list returns).
  - When adding a second mutually-exclusive empty state to a v-if/v-else-if chain, tighten the earlier branch's condition so the more-specific later branch can match — Vue stops at the first true branch. The PRD's 'keep the favourites-only check first' guidance only works because the favourites condition is now strictly favourites-alone.
  - Pinia options-API state Set sizes (`state.selectedRegions.size`) are reactive in templates without any wrapper; the same pattern HomeView already used for `favourites.has(id)`.
---

## 2026-04-19 - US-003 (Phase 11)
- Created ui/src/components/FilterSheet.vue (Vue SFC) wrapping <surf-bottom-sheet title="Filters">. Takes `open: boolean` prop; emits `close` when Done is tapped or the underlying sheet dispatches `sheet-close` (backdrop / Escape).
- Two stacked sections: Region + Difficulty. Each renders native <input type="checkbox"> rows inside <label> wrappers (44px min touch target). Region labels use beachStore.availableRegions verbatim; difficulty labels are capitalise()d display-only while the stored value in selectedDifficulties stays lowercased.
- Change handler on each checkbox calls beachStore.toggleRegion / toggleDifficulty (which already handle add/remove + persistence).
- Footer has two <surf-button>s: 'Clear all' (variant secondary) calls beachStore.clearFilters() without closing the sheet; 'Done' (primary) emits close.
- Empty-state fallback: when availableRegions or availableDifficulties is empty, the section shows a 'No options available' paragraph instead of the list.
- Files changed: ui/src/components/FilterSheet.vue, prd.json.
- **Learnings for future iterations:**
  - Typecheck (`cd ui && yarn typecheck`) passes. Manual browser verification still needed once US-004 adds the trigger button (no entry point yet from HomeView).
  - surf-bottom-sheet emits `sheet-close` (NOT `close`) — Vue wrappers must listen to `@sheet-close` and re-emit `close` upward so consumers see a normal `close` event. The PRD's 'consume the close event from surf-bottom-sheet' wording is slightly misleading; the actual event name is `sheet-close` (see ui/src/components/lit/surf-bottom-sheet.ts:96).
  - Vue `defineEmits` typed signatures use `(e: 'close'): void` call-signature style; TS enforces the emit payload.
  - `accent-color: var(--color-primary)` on checkboxes picks up the existing theme without a custom checkmark component.
---
